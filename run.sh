-- commands to run the application(first app=app.py second is app=FastAPI())

-- uvicorn api:app --reload
-- uvicorn app:app --reload       #resume getting generated and downloaded
-- uvicorn app1:app --reload      #resume getting generated in web